<Window x:Class="IDE.Editar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IDE"
        mc:Ignorable="d"
        Title="Editar" Height="450" Width="318">
    <Grid Background="AliceBlue" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Label Grid.Row="0"
           Grid.ColumnSpan="2"
           Content="Editar Carro"
           FontSize="22"
           FontWeight="Bold"
           HorizontalAlignment="Center"
           Margin="0,10,0,30"/>


        <Label Grid.Row="1" Grid.Column="0" Content="Nome:" VerticalAlignment="Center"/>
        <TextBox x:Name="TextNomeEditar"
             Grid.Row="1"
             Grid.Column="1"
             Height="30"
             Margin="0,5"
             Text="{Binding nome, UpdateSourceTrigger=PropertyChanged}"/>


        <Label Grid.Row="2" Grid.Column="0" Content="Marca:" VerticalAlignment="Center"/>
        <TextBox x:Name="TextMarcaEditar"
             Grid.Row="2"
             Grid.Column="1"
             Height="30"
             Margin="0,5"
             Text="{Binding marca, UpdateSourceTrigger=PropertyChanged}"/>


        <Label Grid.Row="3" Grid.Column="0" Content="Ano:" VerticalAlignment="Center"/>
        <TextBox x:Name="TextAnoEditar"
             Grid.Row="3"
             Grid.Column="1"
             Height="30"
             Margin="0,5"
             Text="{Binding ano, UpdateSourceTrigger=PropertyChanged}"/>


        <Label Grid.Row="4" Grid.Column="0" Content="Preço:" VerticalAlignment="Center"/>
        <TextBox x:Name="TextPrecoEditar"
             Grid.Row="4"
             Grid.Column="1"
             Height="30"
             Margin="0,5"
             Text="{Binding preco, UpdateSourceTrigger=PropertyChanged}"/>


        <Button Grid.Row="6"
            Grid.ColumnSpan="2"
            Content="Salvar"
            Height="40"
            Width="150"
            Background="LightBlue"
            HorizontalAlignment="Center"
            Margin="0,20,0,10"
            Click="Button_SalvarEdicao"/>
    </Grid>
</Window>
